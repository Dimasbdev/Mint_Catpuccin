[Unit]
Description=Kitty Terminal Background Daemon (Instant Launch)
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/kitty-daemon
Restart=always
RestartSec=1
Environment=DISPLAY=:0

[Install]
WantedBy=default.target
